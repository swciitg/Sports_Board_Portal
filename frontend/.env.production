# Used by `npm run build` -- this is what the Docker image bakes in.
# CRA inlines REACT_APP_* at BUILD time, so a wrong value here ships to prod.
REACT_APP_API_BASE_URL="https://swc.iitg.ac.in/sports-board/api"
